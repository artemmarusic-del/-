services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: diabetes_app
    ports:
      - "5432:5432"
    volumes:
      - diabetes_pg_data:/var/lib/postgresql/data

volumes:
  diabetes_pg_data:
